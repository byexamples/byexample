--jobs=2
--pretty=all
--languages=python,shell
--ff
--timeout=4
# disable the default "turn echo off"
# so we have an interpreter echoing the examples
# that we sent to them
-x-turn-echo-off-on-spawn=False
-x-turn-echo-off=False
# force the filtering of the echo
-o=+force-echo-filtering
